spring:
  application:
    name: spring-jdbc-api-demo1

  # เติมเพื่อปิดการทำงานของ web server
  # main:
  #   web-application-type: none

  profiles:
    active: dev

  threads:
    virtual:
      enabled: true